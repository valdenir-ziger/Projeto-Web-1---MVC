<!DOCTYPE html>
<html>
    <head> </head>
    <body>
        <form class="bg-dark text-white" action="/recuperarSenha" method="post"> 
            <input type="hidden" name="id" value={{id}}>
            <div class="form-group col-3">
                <label for="campoLogin">Matrícula/Login</label>
                <input type="text" name="login" value="{{login}}" class="form-control" readonly style="background-color:rgba(177, 175, 175, 0.966)">
            </div>
            <div class="form-group col-3">
                <label for="campoPergunta">Pergunta</label>
                <input type="text" name="pergunta" value="{{pergunta}}" class="form-control" readonly style="background-color:rgba(177, 175, 175, 0.966)">
            </div>
            <div class="form-group col-3">
                <label for="campoResposta">Resposta</label>
                <input type="text" class="form-control" name="resposta">
            </div>
             {{#if excluido}}
                <script> 
                    function voltar() { 
                        window.location.href = "/"
                    }
                </script>
                <button onclick=voltar() class="btn btn-secondary btn-sm">Voltar para tela de login</button>
                <br><br>
                <div class="alert alert-danger col-2" role="alert">login desativado</div>
            {{else}}
                <button type="submit" class="btn btn-secondary btn-sm">Recuperar Senha</button>
            {{/if}}
            
        </form>
        <br>
    </body>
</html>